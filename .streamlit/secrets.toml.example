# ============================================================
# Streamlit Cloud secrets template
# ------------------------------------------------------------
# DO NOT put real keys here and commit them.
# On Streamlit Community Cloud: open your app -> Settings -> Secrets
# and paste the filled-in version of this file there.
#
# For LOCAL use, prefer the .env file instead (already git-ignored).
# A real .streamlit/secrets.toml is also git-ignored for safety.
# ============================================================

MISTRAL_API_KEY = "your-mistral-api-key"
TAVILY_API_KEY  = "your-tavily-api-key"
